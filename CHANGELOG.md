# История изменений

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
версии инструмента следуют [Semantic Versioning](https://semver.org/lang/ru/).
Версии стандарта ведутся отдельно в `SPEC/CHANGELOG.md`.

## [0.12.0] — 2026-08-26

### Добавлено

- mnemo работает и под Codex тем же файлом скила, что и под Claude Code:
  `skills/chat-export` подключается ссылкой в `~/.agents/skills/`, второй копии
  инструкции не заводится. Корень определяется от расположения `SKILL.md` через
  `readlink` — без него относительный путь уезжает в каталог ссылок, а не в
  репозиторий;
- `init --instructions-file AGENTS.md` объявляет архив там, где его прочитает
  Codex. Текст объявления **один и тот же** для обоих файлов: разошедшийся текст
  завёл бы два набора правил на один архив.

### Изменено

- стандарт 1.15: §15.5 говорит про файл инструкций хост-проекта, а не про
  `CLAUDE.md` поимённо.

## [0.11.1] — 2026-08-14

### Исправлено

- reply на пересланного родителя теперь использует подтверждённого исходного
  автора, а не человека, который переслал сообщение;
- скрытое авторство родителя остаётся неподтверждённым.

### Инфраструктура

- добавлен GitHub CI для selfcheck и unit-тестов;
- закреплены правила тегов и GitHub Releases для версий инструмента.

## [0.11.0] — 2026-08-14

### Добавлено

- `people --update` для безопасного дополнения существующей записи;
- импорт и отображение `reply_context` из Herald;
- unit-тесты поведения команд и provenance ответов.

[0.11.1]: https://github.com/ZenonEl/mnemo/compare/v0.11.0...v0.11.1
[0.11.0]: https://github.com/ZenonEl/mnemo/releases/tag/v0.11.0
